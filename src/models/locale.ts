export interface locale {
    _id:string,
    name: string;
    address: string; 
    city: string;
    zipCode: string,
    capacity: number
  }

 export interface LocaleOption {
    _id: string;
    name: string;
    address: string;
  }