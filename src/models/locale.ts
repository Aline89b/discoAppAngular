export interface locale {
    name: string;
    address: string; // event ID or event name
    city: string;
    zipCode: string,
    capacity: number
  }

 export interface LocaleOption {
    _id: string;
    name: string;
    address: string;
  }